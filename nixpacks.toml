[phases.setup]
nixPkgs = ['python311', 'postgresql', 'sqlite', 'poetry']

[phases.install]
cmds = [
  'poetry config virtualenvs.create false',
  'poetry install --only=main --no-dev --no-root'
]

[phases.build]
cmds = ['echo "Build phase completed"']

[start]
cmd = 'python main.py'